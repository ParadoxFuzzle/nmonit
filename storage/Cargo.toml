[package]
name = "compute-storage"
version = "0.1.0"
edition = "2024"
description = "Distributed storage: FUSE filesystem and S3-compatible API"

[dependencies]
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
thiserror = "2"
