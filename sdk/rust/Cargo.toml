[package]
name = "compute-fabric-sdk"
version = "0.1.0"
edition = "2021"
description = "Rust SDK for the distributed compute fabric"

[dependencies]
tonic = { version = "0.12", features = ["tls"] }
prost = "0.13"
tokio = { version = "1", features = ["full"] }
