# Dashboard placeholder

The web dashboard will be implemented in Phase 5 using:
- React 19 + TypeScript
- Tailwind CSS
- Recharts (for resource utilization charts)
- ReactFlow (for topology map)

Features planned:
- Cluster topology map (nodes, links, load heatmap)
- Real-time resource utilization charts
- Job queue and status view
- Per-tenant resource breakdown
- Log viewer
- Alert configuration
