[package]
name = "compute-network"
version = "0.1.0"
edition = "2021"
description = "Network fabric: RDMA/TCP transport layer"

[dependencies]
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
thiserror = "2"
