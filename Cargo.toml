[workspace]
# Phase 1: only the agent is implemented.
# network/, storage/, and sdk/rust/ will be added in later phases.
members = [
    "agent",
]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/compute-nmonit"
